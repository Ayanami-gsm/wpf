<Window x:Class="ClashWpf.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:cal="http://www.caliburnproject.org"
        Title="ClashWpf — mihomo 控制器" Height="620" Width="980" MinHeight="480" MinWidth="760"
        WindowStartupLocation="CenterScreen" Background="#F2F3F5">
    <Window.Resources>
        <Style x:Key="NavButton" TargetType="Button">
            <Setter Property="Margin" Value="12,4"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#E4E7EB"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#D0D5DC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <DockPanel>
        <!-- 顶部标题 + 实时状态 -->
        <Border DockPanel.Dock="Top" Background="#1F2937" Padding="16,8">
            <DockPanel>
                <TextBlock DockPanel.Dock="Left" Text="ClashWpf · mihomo 图形控制器"
                           Foreground="White" FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock Text="{Binding NodeText}" Foreground="#6EE7B7" FontSize="12"
                               Margin="0,0,14,0" VerticalAlignment="Center"
                               ToolTip="当前实际出口节点（取流量最大的活动连接链路末端）"/>
                    <TextBlock Text="{Binding SpeedText}" Foreground="#93C5FD" FontSize="12"
                               Margin="0,0,14,0" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding StatusText}" Foreground="#9CA3AF" FontSize="12"
                               VerticalAlignment="Center" TextTrimming="CharacterEllipsis" MaxWidth="220"
                               ToolTip="{Binding StatusText}"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- 左侧导航 -->
        <Border DockPanel.Dock="Left" Width="200" Background="White" BorderBrush="#E5E7EB" BorderThickness="0,0,1,0">
            <StackPanel Margin="0,16,0,0">
                <Button Style="{StaticResource NavButton}" Content="仪表盘"
                        cal:Message.Attach="[Click] = [ShowDashboard]"/>
                <Button Style="{StaticResource NavButton}" Content="代理节点"
                        cal:Message.Attach="[Click] = [ShowProxies]"/>
                <Button Style="{StaticResource NavButton}" Content="活动连接"
                        cal:Message.Attach="[Click] = [ShowConnections]"/>
                <Button Style="{StaticResource NavButton}" Content="日志"
                        cal:Message.Attach="[Click] = [ShowLogs]"/>
                <Button Style="{StaticResource NavButton}" Content="设置"
                        cal:Message.Attach="[Click] = [ShowSettings]"/>
            </StackPanel>
        </Border>

        <!-- 内容区 -->
        <ContentControl x:Name="ActiveItem" Margin="0,0,0,0"/>
    </DockPanel>
</Window>
